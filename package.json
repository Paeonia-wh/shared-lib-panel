{
  "name": "shared-lib-panel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "front": "node build-front.mjs",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-autostart": "^2.0.0"
  }
}
